---
title: "Webhooks"
description: "Receive real-time events at your own endpoints, with signed, retried deliveries."
icon: "webhook"
---

## Overview

Webhooks push events from Autocampaign AI to your own URL in real time — for example, when a
contact is created or a campaign completes. Manage them at **Settings → Webhooks**
(`/settings/webhooks`).

{/* MEDIA-TODO video | /videos/developers/webhooks-create-and-test-webhook.mp4
Capture: Walkthrough of creating a signed webhook endpoint, sending a test event, and reading the delivery log. Target length: 60–90 seconds.
Script: 1) Go to Settings → Webhooks → New 2) Enter an HTTPS Endpoint URL, a Label, and an optional description 3) Pick two or three events 4) Keep Sign payloads on and save 5) Show the one-time signing secret and copy it (masked in the recording) 6) Click Send test 7) Open the delivery log and show the status, payload, and response of the test delivery
Route: /settings/webhooks
Redact: Use a demo endpoint URL such as https://example.com/webhooks; blur the signing secret in every frame; use a test payload with no real contact names, phone numbers, or emails; hide any custom header values.
Publish: add the file at the path above (or swap the video tag for a YouTube or Loom iframe), then delete this note and the comment markers around the Frame below. */}
{/*
<Frame caption="Create a webhook, send a test, and check the delivery log">
  <video controls className="w-full aspect-video rounded-xl" src="/videos/developers/webhooks-create-and-test-webhook.mp4" title="Walkthrough of creating a signed webhook endpoint, sending a test event, and reviewing the delivery log"></video>
</Frame>
*/}

<Info>
  **Difficulty:** Advanced &nbsp;·&nbsp; **Permission:** Manage webhooks.
</Info>

## Create a webhook

<Steps>
  <Step title="Add an endpoint">
    Go to **Settings → Webhooks → New** and enter your **Endpoint URL** (HTTPS), a
    **Label**, and an optional description.

    {/* MEDIA-TODO screenshot | /images/developers/webhooks-new-endpoint-form.png
    Capture: The new webhook form with the Endpoint URL, Label, and description fields filled with sample values, the event picker with a few events selected, and the Sign payloads toggle on.
    Route: /settings/webhooks (New)
    Redact: Replace the endpoint URL with a demo URL such as https://example.com/webhooks and leave custom header values empty or masked.
    Publish: add the file at the path above, then delete this note and the comment markers around the Frame below. */}
    {/*
    <Frame caption="Add an endpoint and choose its events">
      <img src="/images/developers/webhooks-new-endpoint-form.png" alt="New webhook form with endpoint URL, label, selected events, and a sign payloads toggle switched on" />
    </Frame>
    */}
  </Step>
  <Step title="Subscribe to events">
    Pick the events you want to receive (see below). You can also add custom headers.
  </Step>
  <Step title="Enable signing">
    Keep **Sign payloads** on. On save, your **signing secret** is shown once — copy
    it to verify deliveries.

    {/* MEDIA-TODO screenshot | /images/developers/webhooks-signing-secret.png
    Capture: The one-time signing secret shown after saving a webhook with Sign payloads on, with the copy control visible.
    Route: /settings/webhooks (after saving a new webhook)
    Redact: The signing secret must be masked or replaced with placeholder characters, and the endpoint URL replaced with a demo URL.
    Publish: add the file at the path above, then delete this note and the comment markers around the Frame below. */}
    {/*
    <Frame caption="The signing secret is shown once on save">
      <img src="/images/developers/webhooks-signing-secret.png" alt="One-time webhook signing secret display with a masked value and a copy button" />
    </Frame>
    */}
  </Step>
</Steps>

## Events you can subscribe to

<AccordionGroup>
  <Accordion title="Contacts & segments">
    `contact.created`, `contact.updated`, `contact.deleted`, `contact.tags_changed`,
    `contact.opted_in`, `contact.opted_out`, `segment.created`, `segment.updated`,
    `segment.deleted`
  </Accordion>
  <Accordion title="Campaigns & polls">
    `campaign.created`, `campaign.updated`, `campaign.paused`, `campaign.resumed`,
    `campaign.cancelled`, `poll.created`, `poll.closed`, `poll.expired`
  </Accordion>
  <Accordion title="Inbox & messaging">
    `inbox.message.created`, `inbox.message.status_changed`,
    `inbox.conversation.created`, `inbox.conversation.read`, `inbox.unified.assigned`,
    `inbox.note.created`, `inbound.message.persisted`
  </Accordion>
  <Accordion title="Channels & links">
    `sms_channel.connected`, `sms_channel.disconnected`, `sms_channel.provider_switched`,
    `sms_channel.brand_registered`, `sms_channel.brand_deleted`,
    `sms_channel.campaign_created`, `sms_channel.campaign_deleted`,
    `sms_channel.phone_number_purchased`, `join-link.submitted`, `short-link.clicked`
  </Accordion>
</AccordionGroup>

## Verify signatures

Signed deliveries include these headers:

```http
X-Autocampaign-Signature: t=<unix>,v1=<hex-hmac-sha256>
X-Autocampaign-Timestamp: <unix>
X-Autocampaign-Event: <event.name>
X-Autocampaign-Delivery: <delivery-id>
```

Compute an HMAC-SHA256 of `<timestamp>.<raw-body>` using your signing secret and
compare it (in constant time) to `v1`. Reject stale timestamps.

## Deliveries and retries

- Failed deliveries retry up to **3 attempts** (after ~1 and ~5 minutes), then
  dead-letter.
- An endpoint that fails repeatedly is **auto-disabled** — re-enable it after fixing.
- View the **delivery log** with status, payload, and response, and **retry** failed
  deliveries manually.
- Use **Send test** to fire a sample event and inspect it.

{/* MEDIA-TODO screenshot | /images/developers/webhooks-delivery-log.png
Capture: A webhook's delivery log showing several deliveries with a mix of successful and failed statuses, one delivery expanded to show its payload and response, and the retry and Send test actions visible.
Route: /settings/webhooks (an endpoint's delivery log)
Redact: Replace the endpoint URL with a demo URL, use a test payload with no real contact names, phone numbers, or emails, and mask any signature or secret values in headers.
Publish: add the file at the path above, then delete this note and the comment markers around the Frame below. */}
{/*
<Frame caption="The delivery log shows status, payload, and response">
  <img src="/images/developers/webhooks-delivery-log.png" alt="Webhook delivery log with successful and failed deliveries and one entry expanded to show payload and response" />
</Frame>
*/}

## FAQ

<AccordionGroup>
  <Accordion title="Can I use HTTP (not HTTPS)?">
    Endpoints must be HTTPS. Private, loopback, and internal addresses are blocked for
    security.
  </Accordion>
  <Accordion title="How do I rotate the signing secret?">
    Use the endpoint's rotate-secret action; the new secret is shown once.
  </Accordion>
  <Accordion title="What if my endpoint is down?">
    Deliveries retry a few times, then dead-letter. Persistent failures auto-disable
    the endpoint until you re-enable it.
  </Accordion>
</AccordionGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Signature verification fails">
    Sign `<timestamp>.<raw-body>` (the exact raw bytes, before JSON parsing) with your
    secret using HMAC-SHA256, and compare to the `v1` value in `X-Autocampaign-Signature`.
  </Accordion>
  <Accordion title="My endpoint was auto-disabled">
    It hit repeated terminal failures. Fix the endpoint, then re-activate it — this
    resets the failure streak.
  </Accordion>
</AccordionGroup>

<Card title="API reference" icon="code" href="/api/introduction">
  Explore the full API.
</Card>
