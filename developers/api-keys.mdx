---
title: "API keys"
description: "Create and manage API keys to access the Autocampaign AI API programmatically."
icon: "key"
---

## Overview

API keys let external systems and integrations act on your workspace through the
Autocampaign AI API. Manage them at **Settings → API keys** (`/settings/api-keys`).

{/* MEDIA-TODO video | /videos/developers/api-keys-create-and-rotate-key.mp4
Capture: Walkthrough of creating an API key, copying the secret the one time it is shown, and rotating it. Target length: 60–90 seconds.
Script: 1) Go to Settings → API keys and click Create API key 2) Enter a Name such as Zapier integration, an optional description, and an optional expiration 3) Show the one-time key reveal and click copy (key masked in the recording) 4) Back in the list, point out the active status and last used 5) Click Rotate on the key and show that a new secret is shown once
Route: /settings/api-keys
Redact: Never show a usable key: use a throwaway key that is revoked before publishing AND blur everything after the et_live_ prefix in every frame; hide the real workspace and user name.
Publish: add the file at the path above (or swap the video tag for a YouTube or Loom iframe), then delete this note and the comment markers around the Frame below. */}
{/*
<Frame caption="Create, copy, and rotate an API key">
  <video controls className="w-full aspect-video rounded-xl" src="/videos/developers/api-keys-create-and-rotate-key.mp4" title="Walkthrough of creating an API key, copying the one-time secret, and rotating the key"></video>
</Frame>
*/}

<Info>
  **Difficulty:** Advanced &nbsp;·&nbsp; **Permission:** Manage API keys (human
  users only).
</Info>

<Warning>
  An API key acts with **Owner-level** permissions for your workspace — anyone with
  the key can do anything an owner can. Store it securely and never commit it to
  source control.
</Warning>

## Create an API key

<Steps>
  <Step title="Create key">
    Go to **Settings → API keys** and click **Create API key**.

    {/* MEDIA-TODO screenshot | /images/developers/api-keys-keys-list.png
    Capture: The API keys page with the Create API key button visible and two or three existing keys listed, showing their names, statuses (active, revoked, expired), and last used.
    Route: /settings/api-keys
    Redact: Show only masked key values (nothing beyond the et_live_ prefix) and use demo key names.
    Publish: add the file at the path above, then delete this note and the comment markers around the Frame below. */}
    {/*
    <Frame caption="Create API key is at the top of the API keys page">
      <img src="/images/developers/api-keys-keys-list.png" alt="API keys settings page with a create button and a list of keys showing status and last used" />
    </Frame>
    */}
  </Step>
  <Step title="Name it">
    Give it a **Name** (e.g. "Zapier integration"), an optional description, and an
    optional **expiration** (leave blank for a key that never expires).
  </Step>
  <Step title="Copy the secret now">
    The full key (starting with `et_live_`) is shown **once**. Copy and store it
    immediately — you can't see it again.

    {/* MEDIA-TODO screenshot | /images/developers/api-keys-key-reveal.png
    Capture: The one-time reveal shown right after creating a key, with the copy control and the notice that the key will not be shown again.
    Route: /settings/api-keys (after Create API key)
    Redact: The key must not be readable: use a throwaway key that is revoked before publishing and blur or replace everything after the et_live_ prefix with x characters.
    Publish: add the file at the path above, then delete this note and the comment markers around the Frame below. */}
    {/*
    <Frame caption="The full key is shown once — copy it now">
      <img src="/images/developers/api-keys-key-reveal.png" alt="One-time API key reveal with a masked key, a copy button, and a warning it will not be shown again" />
    </Frame>
    */}
  </Step>
</Steps>

## Authenticate

Send the key on each request using either header:

```http
X-API-Key: et_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

or

```http
Authorization: Bearer et_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

## Rotate, revoke, delete

- **Rotate** — generates a new secret (shown once) and immediately invalidates the
  old one. Use this if a key may be exposed.
- **Revoke** — stops the key working immediately, keeping its history.
- **Delete** — removes it from the list.

Keys show a status of **active**, **revoked**, or **expired**, plus when they were
last used.

<Note>
  API keys can't manage other API keys or webhooks — those actions require a
  signed-in user.
</Note>

## FAQ

<AccordionGroup>
  <Accordion title="Can I scope a key to specific permissions?">
    Not today — every key has Owner-level access. Treat keys as highly sensitive and
    rotate them regularly.
  </Accordion>
  <Accordion title="I lost my key — can I see it again?">
    No. The secret is shown only once. Rotate the key to get a new secret.
  </Accordion>
  <Accordion title="How do I use the API?">
    See the [API reference](/api/introduction) for authentication, conventions, and
    available endpoints.
  </Accordion>
</AccordionGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Requests return 401 Unauthorized">
    Confirm the key is active (not revoked or expired) and sent in the `X-API-Key` or
    `Authorization: Bearer` header exactly as issued.
  </Accordion>
</AccordionGroup>

<Card title="Set up webhooks" icon="webhook" href="/developers/webhooks">
  Receive real-time events.
</Card>
